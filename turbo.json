{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", ".next/**"]
    },
    "lib:build": {
      "dependsOn": ["^lib:build"],
      "outputs": ["dist/**"]
    },
    "test": {},
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "apps:dev": {
      "cache": false
    }
  }
}
